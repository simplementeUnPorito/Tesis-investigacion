# Bitácora — 23 de julio de 2026

**Etapa:** Redacción y revisión del paper URUCOM (*Automatic DC-Offset Calibration for a Geophone Front End*)
**Hito:** Reunión con el tutor + primera pasada de compresión de 6→5 páginas; figura side-by-side GEO→ADC nueva para el paper
**Commits:** 1 en el superproyecto (`2593b86`), que actualiza 3 submódulos: `docs` (`da27749`), `firmware/psoc` (`98b40d6`) y `calculos_modelados/matlab` (`823ebd3`)
**Estado emocional:** El mensaje "el dolor nunca acaba" — usado en los cuatro commits del día (super + 3 submódulos) — es quizás el título más honesto de toda la bitácora. No es un bug de firmware ni una madrugada de debugging; es el desgaste específico de pulir un paper de 5 páginas contra un límite de página duro, revisor simulado incluido, con un tutor real de por medio al final del día.

---

## Contexto del día

Después del cierre arquitectónico del 21 de julio (monolito → 7 submódulos) y sin commits de tesis documentados el 22 (aunque `CONTINUIDAD_REVISION.md` registra que ese día se leyó completo un intercambio con el tutor — "Análisis metodología trabajo" — sin que quedara un commit propio), el 23 de julio es el día en que ese trabajo de revisión cristaliza en tres frentes simultáneos:

1. **El paper mismo** (`docs/Propuesta Urucom/`): una pasada de "compresión de texto" para bajar de 6 a 5 páginas (límite duro de URUCON, incluidas referencias), más un documento nuevo (`REVISION_TUTOR_2026-07-23.md`) preparado específicamente para la reunión con el tutor de esa tarde/noche.
2. **El modelo analítico en MATLAB** (`calculos_modelados/matlab`): dos scripts nuevos para regenerar la Figura 3 del paper (respuesta GEO→ADC) en un layout de dos paneles lado a lado, más el descarte de un conjunto grande de figuras `.fig`/`.png` de barridos de osciloscopio que ya no correspondían a la calibración vigente.
3. **El proyecto PSoC** (`firmware/psoc`): un commit que, a diferencia de los anteriores, no es trabajo de ingeniería — es el IDE de PSoC Creator regenerando archivos derivados (`Generated_Source`, `.svd`, `.rpt`, `.cyfit`) simplemente por haber abierto el proyecto, probablemente para verificar algo relacionado con el datasheet citado en el paper (`[7]`, marcado en rojo en `CONTINUIDAD_REVISION.md` como pendiente de revisión visual).

Es un día donde el "trabajo de tesis" no es escribir código nuevo sino defender, con evidencia cuantitativa, cada afirmación de un paper de cinco páginas frente a un panel de revisión simulado y luego frente al tutor real.

---

## Commits

### `2593b86` (superproyecto) — "el dolor nunca acaba" — 21:40 hs

#### Análisis cuantitativo
| Métrica | Valor |
|---------|-------|
| Archivos modificados | 3 (gitlinks) |
| Submódulos actualizados | `docs`, `firmware/psoc`, `modelado/matlab` |
| Líneas agregadas/borradas | +3/-3 (solo punteros de submódulo) |

#### Diff y análisis técnico
```diff
diff --git a/docs b/docs
-Subproject commit 582e6dbb...
+Subproject commit da277492...
diff --git a/firmware/psoc b/firmware/psoc
-Subproject commit fe453ad3...
+Subproject commit 98b40d68...
diff --git a/modelado/matlab b/modelado/matlab
-Subproject commit 39070780...
+Subproject commit 823ebd3c...
```
Un solo commit "ancla" que sincroniza los tres submódulos con trabajo real del día. Es el patrón habitual desde la migración a submódulos del 21 de julio: el trabajo de ingeniería/redacción ocurre dentro de cada repo hijo y el superproyecto solo registra el snapshot conjunto al final de la sesión.

---

### `da27749` (submódulo `docs`, Vault) — "el dolor nunca acaba" — 21:40 hs

#### Análisis cuantitativo
| Métrica | Valor |
|---------|-------|
| Archivos modificados | 41 |
| Líneas agregadas | +5049 |
| Líneas borradas | -352 |

#### Diff y análisis técnico

**1. Documento nuevo para la reunión: `REVISION_TUTOR_2026-07-23.md` (84 líneas).**
Es la pieza central del día. Resume, en formato de tabla, ocho correcciones ya aplicadas al `.tex` (bug de compilación con una marca `` ```latex `` que había quedado pegada al final de la Sección III; reformulación de la frase de ganancias que mezclaba `2·Gain_PGA` con `K_i`; reclasificación del diagnóstico de tres pares en Board B ×4 como "experimento externo" con números reales — LP 2.98→0.84 %FS, N=3; generalización de la conclusión de "LP-output offset" a "residual DC offsets across the conditioning stages"; orden IEEE de referencias corregido; 12 DOIs agregados; calificador "shared" añadido en todas las comparaciones para no sobrevender la mejora; y un **sign test pareado de una cola nuevo** en §IV-C que da LP menor en 10/10 restarts en las 8 celdas, con p≈10⁻³).

Lo más valioso del documento es la sección 2, un **veredicto de panel de revisión simulado con 5 revisores**: "Minor Revision" para congreso, "Major Revision" si fuera a revista completa, puntaje ponderado ≈65/100, cero problemas críticos. Este tipo de autocrítica estructurada — simular el proceso de peer review antes de someterse a él — es coherente con el patrón que ya aparecía en la bitácora del 15 y 16 de julio ("auto-revisión editorial simulada").

La sección 4 documenta el argumento más fuerte del "Devil's Advocate": las reducciones espectaculares del filtro LP (ej. 45.8→3.2 %FS en Board A ×1) son en parte artefacto de que `c_base` se eligió empíricamente en Board B ×50 y se aplicó sin cambios a todas las demás placas/ganancias — o sea que a A×1 el baseline está garantizado que quede mal ajustado. La mitigación (mantener el calificador "shared" en todas las comparaciones) ya estaba aplicada antes de la reunión; queda como tema abierto para discutir con el tutor si vale la pena una frase que contraste explícitamente contra un trim manual único por placa.

**2. `CONTINUIDAD_REVISION.md` (+222 líneas netas sobre la base existente).**
Este archivo es la "memoria de largo plazo" del proceso de revisión del paper — documenta decisiones ya tomadas para no tener que reconstruir contexto en cada sesión nueva (el mismo patrón de diseño que la propia `MEMORIA_PROGRESO.md` de esta bitácora). La sección agregada el 23 de julio, "Iteración 2026-07-23 — primera base de compresión (solo texto)", registra un cambio de objetivo explícito del autor: **reducir de 6 a 5 páginas** (límite duro de URUCON, refs incluidas), usando cross-references en vez de resúmenes recurrentes, sin tocar todavía geometría de figuras/tablas.

Los cambios de esta pasada se marcaron con `\rev{}` (el protocolo de marcado en rojo acordado con el tutor: todo texto nuevo/modificado queda visualmente distinguido hasta que se apruebe) en cinco secciones: Abstract (reemplazado por versión comprimida del tutor, ~150 palabras, atribuyendo el mayor residual al PGA por su respuesta wideband sin filtrar), Introducción (prior work condensado a 3 líneas: numérico → body-bias → autozero/chopper), la nota de exclusión de Board B ×1/×4 en §IV (comprimida con cross-ref), §V trabajo futuro (dos listas consolidadas en una), y §VI Conclusión (reescrita para no repetir los números del abstract).

El diagnóstico final de esta pasada es honesto sobre sus límites: **el paper compila limpio pero sigue en 6 páginas**. El cuerpo + figuras llenan exactamente 5 páginas, pero las 18 referencias completas caen enteras en la página 6, porque la Figura 3 a ancho completo (`figure*`) ocupa media página 5. La "palanca real" identificada para cerrar la sexta página — achicar la Fig. 3 y/o la Fig. 1 de ancho completo a una columna — queda explícitamente diferida, pendiente de autorización del autor para la próxima pasada. Es un ejemplo claro de ingeniería de restricciones: el problema no es de contenido sino de layout, y el documento lo dice sin rodeos en vez de forzar un recorte de texto que no alcanzaría el objetivo.

**3. Figura nueva: `analog_path_check_sidebyside.png` + script fuente.**
Se agrega una variante de la Figura 3 en layout de dos paneles lado a lado (magnitud | coherencia local), generada por el nuevo script `graficar_geo_adc_sidebyside.m` del submódulo MATLAB (ver abajo). Coexiste con la versión anterior (`analog_path_check.png`, sin sufijo) — probablemente para comparar cuál entra mejor en el espacio disponible dado el problema de páginas recién diagnosticado.

**4. Limpieza y otros artefactos del paper.**
`Propuesta Urucom.zip` se actualiza (snapshot comprimido del directorio completo, probablemente para respaldo o para compartir con el tutor). Se agregan scripts MATLAB nuevos directamente en `docs/Propuesta Urucom/scripts/` (`agressive.m`, `generate_calibration_trace.m`, `plot_waterfall_wide.m`, `refine_analog_response_figure.m`, `refine_analog_response_sidebyside.m`, y una versión "alltogether" de 666 líneas) junto con `waterfall.csv` (2602 líneas) — evidencia de que se generó o refinó también la figura de waterfall/wiggle que aparece en el paper (`waterfall_wiggle_horizontal.png`, que además se recomprime de 893 KB a 238 KB). `Urucom.pdf` se recompila (2.94 MB → 2.20 MB, consistente con la recompresión de imágenes).

#### Impacto en el proyecto
Este es el commit que más directamente mueve la aguja hacia la entrega real del paper: no es una iteración más de redacción sino la preparación explícita para defender el trabajo frente al tutor, con un panel de revisión simulado que ya detectó el punto más débil del argumento (el baseline compartido) y una estrategia de compresión de páginas con un diagnóstico técnico preciso de por qué no alcanza todavía.

---

### `98b40d6` (submódulo `firmware/psoc`) — "el dolor nunca acaba" — 21:39 hs

#### Análisis cuantitativo
| Métrica | Valor |
|---------|-------|
| Archivos modificados | 41 |
| Líneas agregadas | +16470 |
| Líneas borradas | -16369 |

#### Diff y análisis técnico
A pesar del volumen de líneas, este commit **no contiene cambios funcionales de firmware**. Todos los archivos tocados son artefactos generados por PSoC Creator al abrir/compilar el proyecto: `Generated_Source/PSoC5/*` (código C autogenerado de los bloques VDAC — `VDAC_Ref_Sum`, `VDAC_ref_BP`, `VDAC_ref_LP`, `VDAC_ref_PGA`, y sus variantes `_PM`), `cyfitter*.h/.inc` (mapeo de pines/recursos), `cymetadata.c`, `config.hex` (binario compilado), `.svd` (descriptor de periféricos para el debugger), `.rpt` (reporte de fitting) y `TopDesign.cysch` (esquemático, en binario).

El único archivo genuinamente nuevo es `BNC/BNC.v`, un stub de Verilog vacío (25 líneas, todo boilerplate de licencia + comentarios `//`#start/#end`` sin código real en el cuerpo) — el patrón estándar que deja PSoC Creator cuando se agrega un símbolo nuevo al esquemático (en este caso, un símbolo `BNC`, probablemente un conector de referencia para las mediciones del osciloscopio documentadas en el paper) sin implementar lógica custom todavía.

En conjunto, esto sugiere que Elías abrió el proyecto PSoC ese día — probablemente para verificar visualmente algún dato citado en el paper contra el hardware real (coherente con la nota en `CONTINUIDAD_REVISION.md` sobre revisar la cita `[7]` al datasheet del PSoC) — y el IDE regeneró su cache de artefactos derivados como efecto colateral de abrir/compilar, sin que hubiera intención de cambiar el diseño.

#### Impacto en el proyecto
Cero impacto funcional; es ruido de tooling que queda documentado por transparencia (evita que una futura revisión de git-blame interprete este commit como un cambio de hardware).

---

### `823ebd3` (submódulo `calculos_modelados/matlab`) — "el dolor nunca acaba" — 21:39 hs

#### Análisis cuantitativo
| Métrica | Valor |
|---------|-------|
| Archivos modificados | 69 |
| Líneas agregadas | +682 |
| Líneas borradas | -480 |

#### Diff y análisis técnico

**1. Dos scripts MATLAB nuevos en `AnalisisCircuito/`:**

- `graficar_geo_adc_sidebyside.m` (263 líneas): genera la variante de dos paneles (magnitud | coherencia local) de la Figura 3 del paper, guardándola directamente en `docs/Propuesta Urucom/Imagenes/analog_path_check_sidebyside.png`. El script reutiliza el mismo pipeline de datos que `graficar_geofono_vs_geo_adc.m` pero descarta el panel de fase porque "the manuscript body analyzes only magnitude and coherence" — decisión de contenido tomada directamente en el código, reflejando la restricción de espacio del paper.

  Técnicamente, el script cascada la respuesta experimental medida (`PGA_to_ADC.measuredProcessed`, con su magnitud y coherencia local) con el modelo nominal de aceleración del geófono SM-24 (`Hacceleration`, derivado de la función de transferencia de velocidad de segundo orden con `fn=10 Hz`, `zeta=0.25`, `G=28.8`), y superpone un envolvente de Monte Carlo que barre todo el rango 0–2 kΩ del potenciómetro de compensación (`fullPotentiometerEnvelope`). Todo se normaliza contra el máximo de la curva compuesta medida, consistente con el protocolo de normalización que `CONTINUIDAD_REVISION.md` documenta explícitamente para la Figura 3.

- `graficar_geofono_vs_geo_adc.m` (310 líneas): variante hermana, probablemente la versión de tres paneles (magnitud, fase, coherencia) o una comparación centrada en el geófono puro vs. la cadena completa — coherente con el nuevo artefacto `resultados_tanda_calibrada/.../geofono_puro_vs_geo_adc_normalizado.png` que aparece en el mismo commit.

**2. Poda masiva de figuras de barridos de osciloscopio obsoletas.**
Se borran ~25 pares `.fig`/`.png` correspondientes a barridos de osciloscopio segmentados por banda de frecuencia (`Osciloscopio_100mHzto1Hz`, `10Hzto100Hz`, `1kHzto10kHz`, `2.5Hzto25Hz`, `20kHzto200kHz`, `250Hzto2.5kHz`, `500mHzto5Hz`, `50Hzto500Hz`, `5kHzto50kHz`) junto con sus contrapartes de "asentamiento" (`Asentamiento_*`) y varias tablas CSV/TXT en `05_tablas_reportes/` (`configuracion_tolerancias.csv`, `ganancia_medicion_recomendada.csv`, `informe_identificacion.txt`, `limite_slew_rate.csv`, `modelo_operacional_psoc.csv`, `parametros_compensador.csv`, `preprocesamiento.csv`, `resumen_identificacion.csv`, `salud_capturas.csv`, `saturacion_canales.csv`, `uso_capturas_por_etapa.csv`).

Esto es limpieza de una "tanda" de calibración anterior que quedó reemplazada por `resultados_tanda_calibrada/`, donde se agregan las versiones actualizadas de `informe_identificacion.txt` (93 líneas, antes 64) y `resumen_identificacion.csv`, más figuras nuevas: `cadena_geo_adc.fig/png` (en `00_cache` y `04_cadena_adc`), `normalizado_cadena_geo_adc.fig/png`, y `geofono_puro_vs_geo_adc_normalizado.fig/png`. El `RESULTADO.md` de esa carpeta se actualiza (+10/-varios líneas), documentando el resultado consolidado de la tanda de calibración vigente.

**3. `analisis_circuito.mat` (cache binario) se actualiza** — consistente con que los scripts nuevos leyeron y volvieron a escribir el cache tras recalcular las cadenas GEO→ADC.

#### Impacto en el proyecto
Este commit es el soporte cuantitativo detrás de la Figura 3 del paper y de las afirmaciones de coherencia (~0.84 kHz, redondeado a ~1 kHz en el cuerpo) que `REVISION_TUTOR_2026-07-23.md` y `CONTINUIDAD_REVISION.md` documentan. La poda de figuras viejas es señal de que la "tanda calibrada" actual ya es considerada la referencia definitiva, dejando atrás los barridos exploratorios segmentados por banda que sirvieron para construir el modelo pero ya no aportan al paper final.

---

## Balance del día

### Cuantitativo
| Métrica | Valor |
|---------|-------|
| Total commits | 4 (1 superproyecto + 3 submódulos) |
| Líneas agregadas (suma submódulos) | ≈ +22201 |
| Líneas borradas (suma submódulos) | ≈ -17201 |
| Archivos modificados (suma submódulos) | 151 |
| Documentos de revisión nuevos/actualizados | 2 (`REVISION_TUTOR_2026-07-23.md` nuevo, `CONTINUIDAD_REVISION.md` +222 líneas) |
| Scripts MATLAB nuevos | 2 (`graficar_geo_adc_sidebyside.m`, `graficar_geofono_vs_geo_adc.m`) + 6 scripts de soporte dentro de `docs/Propuesta Urucom/scripts/` |
| Cambios funcionales de firmware | 0 (commit de PSoC es 100% regeneración de IDE) |

### Cualitativo

El 23 de julio es un día de tesis que no se mide en líneas de C ni en bugs de hardware, sino en el trabajo — mucho menos visible pero igual de exigente — de convertir un sistema que ya funciona en un argumento científico defendible en cinco páginas. La sesión tiene tres capas claramente diferenciadas por función: `REVISION_TUTOR_2026-07-23.md` es el documento "de cara afuera", preparado para la conversación con el tutor esa misma noche, con un veredicto de panel simulado (Minor Revision, ≈65/100) y el argumento del Devil's Advocate ya anticipado y parcialmente mitigado antes de que nadie lo señalara. `CONTINUIDAD_REVISION.md` es el documento "de cara adentro", la memoria de trabajo que le permite a Elías retomar el hilo de una revisión de meses sin reconstruir contexto cada vez — y que registra, con total honestidad, que la primera pasada de compresión de texto **no alcanzó el objetivo**: el paper sigue en 6 páginas porque el problema real es de geometría de figuras, no de prosa, y esa decisión (achicar la Fig. 3 full-width) queda deliberadamente diferida hasta tener autorización explícita del autor.

Los dos scripts MATLAB nuevos y la poda de figuras viejas muestran que el trabajo de revisión editorial no fue solo textual: se regeneró activamente el respaldo cuantitativo de la Figura 3 (probando un layout de dos paneles side-by-side como alternativa al de tres paneles) y se consolidó la "tanda calibrada" vigente descartando docenas de figuras exploratorias de barridos segmentados por banda que ya cumplieron su función en el proceso de identificación del modelo.

El commit de PSoC, en cambio, es puro ruido de tooling — abrir el proyecto para chequear algo (probablemente una referencia de datasheet) dispara una regeneración completa de artefactos derivados sin que haya intención de tocar el hardware. Vale la pena documentarlo así explícitamente para que quede claro que no representa trabajo de ingeniería de firmware ese día, a diferencia de los commits de PSoC que sí lo fueron en jornadas anteriores (ej. 20 y 21 de julio).

El mensaje "el dolor nunca acaba", repetido idénticamente en los cuatro commits, es revelador del estado de ánimo: no es la frustración puntual de un bug (como los "AYUDAAAAA" o "XD" de sesiones de firmware), sino el desgaste acumulativo y algo resignado de pulir un texto académico contra un límite de páginas duro, sabiendo que después de esta pasada todavía queda una reunión con el tutor por delante y, según el propio documento, una decisión de layout pendiente de autorización antes de poder cerrar el problema de la sexta página.

---

← [[2026-07-21]] — Ruptura arquitectónica: monolito → superproyecto con 7 submódulos; refactor AMux dedicados por etapa en `psoc` y modelo de circuito calibrado con `Ru=6.8kΩ` real en `matlab`
→ [[2026-07-24]] — Consolidación de `src/` por propósito (no lenguaje), `investigacion` migra a `docs/investigacion`, plan de conectividad ENLACE del maestro
