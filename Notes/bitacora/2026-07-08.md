# Bitácora — 8 de julio de 2026

**Etapa:** Firmware PSoC/ESP32 estabilizado tras el cierre del 7/7 + apertura de una superficie nueva de trabajo en Python (revisión de campo + MASW multimodal)
**Hito:** El ADC del PSoC pasa de 2 a 4 configs de rango de entrada (todas validadas en hardware con el comando de banco `range N`), la web del maestro suma un indicador de intensidad de señal (RSSI) para el enlace ESP-NOW y el WiFi de la interfaz, y `review_field_data.py` recibe doce fases de pulido (orden por pico a pico, filtros, alineación entre tandas, waterfall f-k, picks de MASW)
**Commits:** 3 (mismo mensaje, mismo timestamp, en tres submódulos distintos)
- `92ae3c42` — `src/firmware/psoc` — "Casi casi" — 14:35:45 -0300
- `2be189e9` — `src/firmware/esp32` — "Casi casi" — 14:35:45 -0300
- `0ca71d9f` — `src/interfaces/python` — "Casi casi" — 14:35:45 -0300
**Estado emocional:** El mismo "Casi casi" del 7 de julio, repetido al segundo exacto en los tres submódulos. A diferencia de aquel día, acá no hay una lista de bugs de campo pendiente de cerrar — el "casi" parece describir más bien un estado de trabajo en curso sobre un documento de handoff de doce fases (`HANDOFF_FIELD_REVIEW.md`), la mayoría ya marcadas `Hecho`, sin el peso emocional de una regresión sin resolver.

---

## Contexto del día

El 7 de julio había cerrado con el hallazgo del circuit-breaker de tormenta de IRQ y la validación en hardware del build de "políticas de eventos determinismo-primero". La sesión del 8 de julio retoma sobre esa base estable en dos frentes que no dependen entre sí:

1. **Firmware (PSoC + ESP32):** ampliar el rango dinámico configurable del ADC de 2 a 4 opciones, y agregar visibilidad de la calidad del enlace radial en la web del maestro — dos features de "terreno" pedidas para hacer más robusta la operación en campo, no bugs.
2. **Interfaces Python:** un salto grande en `review_field_data.py`, la herramienta de revisión de las capturas exportadas de `Canchita`, documentado íntegramente en un archivo de handoff nuevo (`HANDOFF_FIELD_REVIEW.md`, 703 líneas) pensado explícitamente para que otra sesión (o el propio autor, días después) pueda retomarlo sin perder contexto.

Los tres commits comparten el mismo mensaje y el mismo segundo de timestamp (14:35:45), el mismo patrón de "guardar todo el estado del repo de una sola sesión asistida por agente" que ya apareció el 2 y el 7 de julio — pero esta vez sin el commit de revisión manual posterior.

---

## Commits

### `92ae3c42` — `src/firmware/psoc` — "Casi casi" — 14:35:45 hs

#### Análisis cuantitativo
| Métrica | Valor |
|---------|-------|
| Archivos modificados | 349 (la enorme mayoría son artefactos regenerados de PSoC Creator: `.lst`, `.o`, `.hex`, `.map`, `codegentemp/*`) |
| Archivos de código/documentación tocados a mano | 5 (`main.c`, `psoc_adc.c`, `psoc_adc.h`, `psoc_hw.h`, `BUILD_PROGRAM_PSOC.md`) |
| Líneas agregadas (total, incl. generados) | +212119 |
| Líneas borradas (total, incl. generados) | -243610 |
| Líneas escritas a mano (estimado, los 5 archivos reales) | ~150 |

#### Diff y análisis técnico

El cambio funcional es chico y quirúrgico, escondido detrás de un diff gigante porque PSoC Creator regenera el proyecto entero (`.cydsn`) cada vez que se toca el customizer del componente `ADC` en el TopDesign — de ahí los 349 archivos y las +212k/-243k líneas, casi todas binarios de build (`.o`, `.a`, `.elf`) o archivos de código generado (`cyfitter*.h`, `codegentemp/*`) que ni siquiera están pensados para leerse en diff.

Lo que efectivamente cambió, y lo que importa:

```c
// main.c — dma_buf_to_adc_counts()
-    if (ADC_Config == ADC_CF_0V512) {
+    if (ADC_Config != ADC_CF_2V5) {
         return sign_extend_bits(u, (uint8)ADC_CF_0V512_RESOLUTION);
     }
```

Antes, la extensión de signo para el caso "right-aligned" del ADC_DelSig solo se aplicaba si la config activa era exactamente `ADC_CF_0V512` (la única config de rango angosto que existía además de la default `CF_2V5`). Al agregar dos configs nuevas (`CF_1V024`, `CF_0V625`) que comparten la misma resolución de 18 bits y el mismo alineamiento a la derecha que `CF_0V512`, la condición había que generalizarla: "cualquier config que no sea `CF_2V5`" en vez de "la config número 2 específicamente". Es el tipo de bug de "generalización a medias" que aparece típicamente cuando se agrega la tercera opción a un `if` que fue escrito pensando solo en dos casos — acá se atajó en el mismo commit que agrega las opciones, no como fix posterior.

```c
// psoc_adc.c
 uint8 psoc_adc_set_config(uint8 cfg)
 {
-    if ((cfg != ADC_CF_2V5) && (cfg != ADC_CF_0V512)) {
+    if ((cfg != ADC_CF_2V5) && (cfg != ADC_CF_0V512) &&
+        (cfg != ADC_CF_1V024) && (cfg != ADC_CF_0V625)) {
         return 0u;
     }
```

El validador de config del firmware del PSoC —el que decide si acepta o rechaza (`ok=0`) el comando `PSOC_CMD_ADC_CONFIG` (0xBA) que llega por UART desde el ESP esclavo— se extiende para aceptar las 4 configs. Antes de este cambio, las configs 3 y 4 del componente `ADC` (que ya existían a nivel de hardware/TopDesign, con `DEC_DIV=0` como la 2, a diferencia de la 1 que usa `DEC_DIV=32`) estaban deliberadamente excluidas de la aplicación para no romper la unicidad de la Fs del sistema. El comentario viejo en `psoc_adc.h` decía explícitamente "las configs 3/4 del componente (976 SPS) quedan fuera de la aplicación" — es decir, en algún punto entre el diseño original del componente y este commit, las 4 configs pasaron a converger todas a 1020 SPS / 18 bits, lo que elimina la razón original para excluir 3 y 4.

`psoc_hw.h` actualiza solo el comentario del comando `PSOC_CMD_ADC_CONFIG` para documentar los 4 valores válidos de parámetro — un cambio cosmético pero necesario porque ese comentario es la única "spec" del protocolo serie entre PSoC y ESP esclavo, y designers futuros (o el propio autor en unas semanas) lo van a leer antes que el código.

`BUILD_PROGRAM_PSOC.md` es, como en días anteriores, el diario de bitácora técnico del propio firmware: documenta la tabla de las 4 configs con su rango, alineamiento y referencia interna, lista exactamente qué archivos de firmware hubo que tocar para soportarlas (incluyendo el ESP esclavo y la web, que viven en otros submódulos), y — el dato más importante del commit — registra que el build **se programó y se validó en hardware real** el 7/7 a las 12:37:50 (`Flash/SRAM: 41982/49352 bytes`, filas `0..163`), con las 4 configs completando captura limpia (`bBad=0, fill=1/1, ack ok=1`) usando el ESP esclavo en `COM12` y un comando USB nuevo (`range N`) que permite forzar cualquiera de las 4 configs sin pasar por el maestro — útil para aislar el ADC del resto del sistema al probar.

Un detalle documentado con cuidado en el mismo archivo: el PSoC corre auto-calibración al arrancar o cambiar de config (`PSOC_AUTO_CAL_ON_READY=1`), y si se manda `cap` mientras la calibración todavía está en curso (`[CAL] busy` en el log), la captura queda trabada en `START_SYNC_STALL` y ni `clear` la saca de ahí — hace falta `stop` explícito. El autor aclara que esto **no es un bug de las configs nuevas**, es un problema preexistente que solo se nota si se apura la primera captura tras el boot — una nota de las que evitan que alguien pierda una hora re-descubriendo el mismo problema.

#### Impacto en el proyecto

Duplica el rango dinámico configurable del sistema (de 2 a 4 opciones de fondo de escala: ±2.5 V, ±0.512 V, ±1.024 V, ±0.625 V), todo a la misma Fs efectiva de 1020 Hz — lo que significa que ampliar el rango no introduce ningún costo de re-sincronización entre nodos. Para MASW esto importa directamente: distintas distancias fuente-receptor producen amplitudes muy distintas (las capturas de Enfase/`review_field_data.py` del mismo día muestran p2p de ~0.35-0.40 V a 10-14 m contra ~0.04-0.08 V a 46-50 m), así que tener más granularidad de rango permite exprimir mejor la resolución del ADC de 18 bits en cada distancia en vez de forzar todas las tomas al mismo fondo de escala de ±2.5 V.

---

### `2be189e9` — `src/firmware/esp32` — "Casi casi" — 14:35:45 hs

#### Análisis cuantitativo
| Métrica | Valor |
|---------|-------|
| Archivos modificados | 9 |
| Líneas agregadas | +334 |
| Líneas borradas | -16 |
| Archivo nuevo | `master/src/link_rssi.h` (146 líneas) |

#### Diff y análisis técnico

Tres cambios independientes conviven en este commit: el reflejo web de las 4 configs de ADC del commit anterior, un indicador de intensidad de señal de enlace, y un guard de `beforeunload` para no perder datos sin exportar.

**1. Reflejo de las 4 configs en la web** (`config.js`):

```js
export const ADC_CONFIGS = [
   { code: 1, label: '±2.5 V', rangeV: 2.5, fsHz: 1020, countsPerVolt: 131072 / 2.5 },
   { code: 2, label: '±0.512 V', rangeV: 0.512, fsHz: 1020, countsPerVolt: 131072 / 0.512 },
+  { code: 3, label: '±1.024 V', rangeV: 1.024, fsHz: 1020, countsPerVolt: 131072 / 1.024 },
+  { code: 4, label: '±0.625 V', rangeV: 0.625, fsHz: 1020, countsPerVolt: 131072 / 0.625 },
 ];
```

El dropdown de la UI ya era genérico (itera el array, no había que tocar `slave_panel.js`), pero sí apareció un clamp viejo en `app.js` que limitaba el valor restaurado de `localStorage` al rango `[1,2]` — corregido a `[1, cfg.ADC_CONFIGS.length]`. Es un bug latente clásico de "hardcodear el límite actual en vez de derivarlo de la fuente de verdad": funcionaba mientras solo había 2 configs, y se hubiese roto silenciosamente (clamp a 2 aunque el usuario pida 3 o 4) si no se corregía en el mismo commit.

También se corrigió una condición de carrera de protocolo en `onSendAll`:

```js
+  // El esclavo solo atiende un config del PSoC a la vez (g_cfg_waiting en
+  // main.cpp): si el rango ADC se manda mientras el PGA todavia espera su
+  // ACK, el esclavo lo rechaza en el acto (cfg busy, ok=0) y el ADC nunca
+  // llega a aplicarse.
```

Antes, PGA y rango ADC se mandaban en paralelo, sin esperar el ACK del primero. El esclavo solo puede procesar un comando de configuración a la vez (`g_cfg_waiting`), así que si el ADC llegaba mientras el PGA seguía pendiente, el esclavo lo rechazaba silenciosamente. La solución es un polling liviano (`setTimeout` cada 50 ms) que espera a que el pending de PGA se resuelva —por ACK o por timeout, lo que ocurra primero— antes de mandar el comando de rango ADC. Es un fix de coordinación de protocolo, no de las 4 configs en sí, pero probablemente solo se hizo visible al ejercitar más combinaciones de "enviar todo" con las nuevas opciones.

**2. Intensidad de señal (RSSI)** — el cambio más grande y nuevo del día en este submódulo, todo en `link_rssi.h`:

```c
static void linkRssiPromiscuousCb(void *buf, wifi_promiscuous_pkt_type_t type)
{
    if (type != WIFI_PKT_MGMT) return;
    ...
    if (frameType != 0 || frameSubtype != 0xD) return;   /* solo Action frames (ESP-NOW) */
    const uint8_t *src = p + 10;   /* addr2 = 2(fc)+2(dur)+6(addr1) */
    for (uint8_t n = 1; n <= NUM_SLAVES; n++) {
        if (g_cachedHello[n].valid && memcmp(src, g_cachedHello[n].mac, 6) == 0) {
            g_slaveLinkRssi[n].rssi = pkt->rx_ctrl.rssi;
            ...
```

El problema que resuelve este archivo es una limitación de la librería ESP-NOW del core de Arduino/ESP-IDF que se usa: el callback normal de recepción no expone el RSSI del paquete. La solución es poner el radio en modo promiscuo, filtrando solo frames de gestión (`WIFI_PKT_MGMT`) y, dentro de esos, solo "Action frames" (subtype `0xD`, que es el tipo de frame que usa ESP-NOW bajo el capó) cuya MAC origen coincide con la de un esclavo ya conocido (aprendida vía el `HELLO` inicial). Es un hack de bajo nivel pero acotado: sniffea el aire pasivamente, no interfiere con la recepción normal de ESP-NOW.

Lo más cuidado del diseño es la política de cuándo el sniffer está activo:

```c
inline void linkRssiLoop()
{
    bool acquisitionActive = (g_state == PRESTART) || (g_state == RUNNING) ||
                              (g_state == STOPPING) || (g_state == DUMPING) ||
                              (g_state == SCOPE_MULTI);
    bool wantPromiscuous = !acquisitionActive;
    linkRssiSetPromiscuous(wantPromiscuous);
    if (!wantPromiscuous) return;
```

Durante la ventana crítica de captura (`PRESTART`/`RUNNING`/`STOPPING`/`DUMPING`/`SCOPE_MULTI`), el modo promiscuo se **apaga por completo**, no solo se deja de reportar — decisión explícita para no competir por radio/CPU justo cuando el sistema necesita todo el ancho de banda de ESP-NOW para el store-and-forward de las muestras. La UI simplemente se queda con el último valor mostrado (`age_ms` en el JSON permite a futuro decidir si el dato es "fresco" o no, aunque hoy solo se usa para marcar `null` si pasaron más de 15 s).

Se agregan dos indicadores distintos en la web: RSSI del enlace WiFi entre la interfaz (celular/PC) y el AP del maestro (`esp_wifi_ap_get_sta_list`), en la barra de estado global, y RSSI por nodo del enlace ESP-NOW esclavo↔maestro, en cada panel de esclavo. El maestro reporta ambos por WebSocket cada 3 segundos.

**3. Guard de `beforeunload`** (`app.js`):

```js
+function hasUnexportedData() {
+  for (const capture of preservedCaptures) {
+    if (!exportedCaptureSignatures.has(capture.signature)) return true;
+  }
+  const liveCapture = makeCaptureSnapshot('Actual', { source: 'live', visible: false });
+  return captureHasSamples(liveCapture) && !exportedCaptureSignatures.has(liveCapture.signature);
+}
...
+window.addEventListener('beforeunload', (e) => {
+  if (!hasUnexportedData()) return;
+  e.preventDefault();
+  e.returnValue = 'Hay mediciones capturadas sin exportar. Si salis ahora se pierden.';
+});
```

El comentario en el propio código es elocuente: "perderlas silenciosamente ya causó problemas en campo". Es un fix reactivo a un incidente real de pérdida de datos, no una feature especulativa. El diseño evita el error clásico de este tipo de guard —un flag manual que queda desincronizado— recalculando `hasUnexportedData()` al vuelo contra un `Set` de firmas (`exportedCaptureSignatures`) que solo se llena tras un `downloadBlob` exitoso en `onExportRequested`, así que no hay ningún lugar donde el estado "ya exportado" y el guard puedan divergir.

Todos los cambios de este submódulo (RSSI y `beforeunload`) están marcados como **pendientes de validar con hardware real** en `WEB_FIELD_TESTS.md`, porque requieren reflashear el maestro (COM8), lo que corta el WiFi de la interfaz y exige que el usuario esté presente para reconectarse — una limitación operativa de campo, no técnica, que retrasa la validación.

#### Impacto en el proyecto

El indicador de RSSI convierte un problema típico de operación de campo con radio (¿por qué este nodo pierde paquetes, está lejos o hay algo raro?) en algo visible en tiempo real en vez de tener que inferirlo de logs post-mortem. El guard de `beforeunload` cierra una clase entera de incidentes de pérdida de datos por error humano (cerrar la pestaña sin exportar), que es exactamente el tipo de bug "tonto" que en campo, después de horas de trabajo, es fácil de cometer.

---

### `0ca71d9f` — `src/interfaces/python` — "Casi casi" — 14:35:45 hs

#### Análisis cuantitativo
| Métrica | Valor |
|---------|-------|
| Archivos modificados | 7 |
| Líneas agregadas | +4217 |
| Líneas borradas | -305 |
| Archivos nuevos | `HANDOFF_FIELD_REVIEW.md` (703 líneas), `masw_multimodal.py` (168 líneas) |
| Archivo más tocado | `field_review_app.py` (+2810/-¿? de una base de varios miles de líneas) |

#### Diff y análisis técnico

Este es, con diferencia, el commit más grande del día y uno de los más grandes de todo el proyecto hasta la fecha en volumen de código Python escrito a mano (no generado). El propio commit documenta su alcance en `HANDOFF_FIELD_REVIEW.md`, un archivo de handoff de 703 líneas escrito explícitamente para que "otra sesión (Codex u otra)" pueda retomar el trabajo sin re-derivar contexto — un patrón de documentación notablemente maduro para un proyecto de tesis individual, que sugiere que el autor ya anticipa trabajar con asistencia de agentes en sesiones separadas y quiere que cada una arranque "en frío" con el estado completo.

El objetivo declarado en la cabecera del handoff resume bien la motivación:

> "guiar la calibración empezando por la señal más fácil de ver (mayor pico a pico), no dar nada por 'ok' sin revisión manual, mostrar en vivo el promedio de lo aprobado, y dar control explícito (no implícito) sobre inversión de polaridad, inclusión de trazas en el waterfall y picks de MASW."

Doce fases, casi todas marcadas `Hecho` en la tabla de estado del handoff:

**Fase 1-2 — orden por pico a pico.** Se agrega un helper puro `peak_to_peak(samples)` y se usa para reordenar la tabla de capturas de mayor a menor amplitud (en vez del orden de carpeta/captura original), bajo la premisa de que conviene calibrar el cursor de trigger primero sobre las señales más fáciles de ver. Detalle técnico cuidado: el pico a pico es invariante ante la inversión de polaridad (`max - min` no cambia si se invierte el signo), así que el caché de p2p no necesita invalidarse cuando el usuario invierte una señal — evita un recálculo costoso en cada toggle.

**Fase 3 — gateo por revisión real.** Se descubre que existía un campo `PickAnnotation.reviewed` (default `False`) que ya se seteaba correctamente al guardar, pero que el cálculo de promedios y waterfall solo miraba `accepted` (default `True`) — es decir, todo lo no revisado entraba igual al promedio. El fix es gatear por `reviewed and accepted` en vez de solo `accepted`.

**Fase 6 — bug de orden de filtro > 4.** Diagnóstico documentado explícitamente antes de tocar código: "el orden 4 de Filtros NO está clampeado en ningún lado (...) el único lugar donde aparece '4' es un hint de texto, no una restricción funcional" — candidato principal señalado de antemano: `filtfilt` con `padlen` mayor que la longitud de la señal en capturas cortas de 3 s @ 2929 Hz.

**Fase 7/11b — Enfase entre tandas.** La pestaña nueva `Enfase` corrige desalineamientos temporales chicos entre grupos de capturas tomados en días distintos con el mismo label de distancia, con offset configurable por señal individual (no por carpeta, decisión explícita del usuario aunque "no recomendada"), persistido en `alignment_offsets.json`.

**Fase 12 — filtro f-k direccional en el waterfall,** para eliminar rebotes/reflexiones no deseadas de la visualización — la fase más avanzada en términos de procesamiento de señal de todo el handoff.

Además del handoff y las fases sobre `field_review_app.py`/`field_review_data.py`, aparece `masw_multimodal.py` (168 líneas, nuevo), que agrega inversión conjunta multi-modo de curvas de dispersión Rayleigh usando `disba` (forward model, acelerado con Numba) + `evodcinv` (inversión evolutiva CPSO), ambas dependencias opcionales:

```python
def available() -> bool:
    """True si disba y evodcinv se pueden importar."""
    try:
        _patch_numpy()
        import disba
        import evodcinv
        return True
    except Exception:
        return False
```

Con una nota de compatibilidad interesante: `evodcinv` 2.2.x todavía usa `np.Inf` (removido en NumPy 2.0), así que `_patch_numpy()` reinyecta los alias viejos (`Inf`, `NaN`, `Infinity`, etc.) antes de importar la librería — un parche de compatibilidad entre dependencias de terceros con ciclos de vida desincronizados, resuelto sin fijar versiones antiguas de NumPy.

El `README_python.md` documenta en prosa varios de estos cambios para el usuario final de la herramienta, incluyendo una nota clave sobre **campañas con Fs distinta**: las capturas del 3/7 (3 s @ 2929 Hz) y las del 7/7 (10.59 s @ 1020 Hz, ventana larga para ver bajas frecuencias ~1 Hz) conviven en el mismo dataset. La solución es resamplear (`resample_poly`) cada grupo de distancia a una Fs común (la mínima del grupo, o una fijada manualmente) y alinear por trigger, dejando en NaN la cola que solo las capturas largas alcanzan a cubrir — "nunca se descarta un grupo por tener fs mezcladas".

#### Impacto en el proyecto

Este commit abre, de facto, la segunda mitad del pipeline de la tesis: hasta ahora el trabajo se concentraba en adquirir señal limpia (firmware, hardware, protocolo); acá aparece por primera vez con este nivel de profundidad la cadena de post-procesamiento completa — revisión manual asistida, alineación entre campañas, filtrado, generación de curvas de dispersión y inversión multi-modo para obtener el perfil de velocidades de onda de corte, que es el resultado final que persigue todo el sistema MASW. Es, en superficie de código nueva, el salto más grande del proyecto hasta la fecha.

---

## Balance del día

### Cuantitativo
| Métrica | Valor |
|---------|-------|
| Total commits | 3 |
| Submódulos tocados | 3 (psoc, esp32, interfaces/python) |
| Líneas agregadas (total, incl. binarios/generados de PSoC) | ~216670 |
| Líneas borradas (total, incl. binarios/generados de PSoC) | ~243931 |
| Líneas de código/documentación escritas a mano (estimado) | ~4700 |
| Archivos nuevos | 3 (`link_rssi.h`, `HANDOFF_FIELD_REVIEW.md`, `masw_multimodal.py`) |

### Cualitativo

El 8 de julio tiene una estructura de dos velocidades muy distinta. Del lado del firmware (PSoC + ESP32), el trabajo es de ampliación controlada sobre una base ya validada el día anterior: duplicar las configs de ADC de 2 a 4, corregir en el mismo commit los dos lugares donde el código asumía implícitamente "solo hay 2 opciones" (la extensión de signo generalizada a "todo lo que no sea CF_2V5", el clamp de `localStorage` en la web), agregar visibilidad de RSSI con una política de apagado explícito durante captura para no competir por radio, y cerrar un guard de `beforeunload` motivado por un incidente real de pérdida de datos en campo. Es trabajo cuidadoso, acotado, ya con la config de 4 rangos validada en hardware real el 7/7 vía el comando de banco `range N` — solo el RSSI y el guard quedan pendientes de reflash y prueba en campo real.

Del lado de interfaces Python, en cambio, el commit es de una escala completamente distinta: 4217 líneas agregadas en un solo commit, doce fases de una herramienta de revisión de campo documentadas en un handoff de 703 líneas pensado para sobrevivir el cambio de sesión, y la aparición de un módulo de inversión conjunta multi-modo (`masw_multimodal.py`) que apunta directamente al resultado final de la tesis: el perfil de velocidades de onda de corte a partir de curvas de dispersión Rayleigh. El nivel de detalle del handoff —qué se investigó antes de tocar código, qué bugs preexistentes se encontraron de paso (el de `_apply_distance_to_folder` usando índice de dataset como si fuera fila de tabla), qué decisiones del usuario fueron "no recomendadas pero elegidas igual" (offset de Enfase por señal en vez de por carpeta)— retrata a alguien que trabaja de forma consciente sobre la deuda de contexto que se acumula entre sesiones largas de desarrollo asistido.

El "Casi casi" que titula los tres commits, idéntico y simultáneo como el patrón ya visto el 2 y el 7 de julio, esta vez no parece cargar la tensión de una regresión sin resolver: la mayoría de las fases del handoff de Python están en `Hecho`, y el firmware ya tiene su validación en hardware documentada desde el día anterior. Es más bien el "casi" de un cierre de sesión con trabajo sustancial completado pero con la validación de campo —reflash del maestro con RSSI y `beforeunload`, más prueba real de las 4 configs en operación normal (no solo por USB directo)— todavía pendiente.

---

← [[2026-07-07]] — circuit-breaker de tormenta de IRQ y cierre de las dos regresiones de campo del 6/7
→ [[2026-07-09]] — auditoría autocrítica de las Fases 9-11 y grupos de dispersión con combinación ponderada
